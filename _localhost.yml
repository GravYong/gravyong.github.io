# Local-dev overrides, layered on top of _config.yml via:
#   bundle exec jekyll serve --config _config.yml,_localhost.yml
#
# Only put settings here that MUST differ from production. Everything
# else (author identity, plugins, defaults, social handles, etc.) is
# inherited from _config.yml so the two never drift apart.

url      : "http://127.0.0.1:4000"
baseurl  :
